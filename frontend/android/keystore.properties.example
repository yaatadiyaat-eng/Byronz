# Salin file ini menjadi `keystore.properties`, lalu isi nilai sebenarnya.
# Simpan file `keystore.properties` asli di mesin Anda dan jangan commit ke repo publik.

storeFile=release-keystore.jks
storePassword=GANTI_DENGAN_PASSWORD_KEYSTORE
keyAlias=byronz-release
keyPassword=GANTI_DENGAN_PASSWORD_KEY
